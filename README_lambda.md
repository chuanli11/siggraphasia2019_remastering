# Install

```
conda create python=3.6 -p /ParkCounty/peDev/conda/envs/remaster-lambda

conda install pytorch torchvision cudatoolkit=10.1 -c pytorch -n remaster-lambda

conda install x264 ffmpeg -c conda-forge -n remaster-lambda

pip install opencv-python

pip install scikit-image

pip install tqdm

```